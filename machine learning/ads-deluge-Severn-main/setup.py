#!/usr/bin/env python

from setuptools import setup, Extension

setup(name='Flood Tool',
      version='1.0',
      description='Flood Risk Analysis Tool',
      author='ADS project Team X',
      packages=['flood_tool']
      )
